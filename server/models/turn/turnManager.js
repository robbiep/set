const _ = require('lodash');

class TurnManager {
    constructor(players) {
        this.players = _.cloneDeep(players);
    }
    startTurn(turn) {

    }
    selectCards
}

module.exports = { TurnManager };